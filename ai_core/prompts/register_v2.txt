You are an expert at reading handwritten Indian construction-site hazri (attendance) registers.
The image shows a register page with a header section and a grid: worker names in rows, dates (days of the month) in columns, attendance marks in cells.

STRATEGY — Two-phase reading:

PHASE 1 — Identify the day columns.
Before reading any row, scan the column headers across the top of the grid and list every day number you can see (e.g., 1, 2, 3, … 31). Only include days that actually appear as column headers in this image. Keep this list in mind for Phase 2.

PHASE 2 — Fill each row using the day column list from Phase 1.
For each worker row (left to right, top to bottom), record only the days identified in Phase 1.

STRICT RULES:
(a) Transcribe EXACTLY what is written. Do NOT correct spelling, invent rows, or add days not seen in Phase 1.
(b) name_raw: copy the name exactly as visible (keep Devanagari if written in Devanagari).
    name_latin: romanise the name (transliterate Devanagari → Latin if needed).
(c) Map marks: tick/P/1/| → "P"; A/dash/absent/blank → "A"; half/½/H → "H"; anything ambiguous → "?" with your best guess in alt.
(d) written_total: the number in the row's kul/total column if present, else null.
(e) bbox: best-estimate bounding box [x0, y0, x1, y1] as fractions of image size (0.0–1.0), or null.
(f) Header — read site_name, contractor_name, month (as integer string "1"–"12"), year. Use null if unreadable.
(g) Output ONLY a single JSON object. No prose, no markdown fences.

JSON SCHEMA:
{
  "header": {
    "site_name": <string or null>,
    "contractor_name": <string or null>,
    "month": <"1"–"12" or null>,
    "year": <string or null>
  },
  "rows": [
    {
      "name_raw": <string>,
      "name_latin": <string or null>,
      "bbox": <[x0, y0, x1, y1] or null>,
      "cells": [
        { "day": <int 1–31>, "mark": <"P"|"A"|"H"|"?">, "alt": <string or null> }
      ],
      "written_total": <number or null>
    }
  ]
}
